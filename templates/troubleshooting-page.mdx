---
title: Why can't I [the symptom, as the user would type it]?
description: The one-line answer — cause first.
---

{/*
  TROUBLESHOOTING PAGE TEMPLATE — lives in docs/troubleshooting/.
  Titled by symptom, structured symptom → cause → fix.
  These pages are read by someone blocked and possibly annoyed:
  zero personality budget, answer in the first line, shortest page
  type in the guide.
*/}

# Why can't I [symptom]?

**Because [the cause, in one sentence].** [One more sentence of context if
the cause needs it — usually the design reason Flow behaves this way.]

## The fix

1. The steps that unblock them, numbered, shortest path only.
2. Link the task page for the full picture rather than duplicating it.

## If that wasn't it

Optional: the second-most-likely cause, one line, with its fix or link.
Two causes maximum — a page needing three is really two pages.

## Related

- [The concept page that explains the design](/docs/section/page)
