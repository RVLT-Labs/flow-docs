---
title: Verb-first task title
description: One sentence — what this accomplishes, in the product's voice. Shows in search results and section cards.
---

{/*
  TASK PAGE TEMPLATE — copy, fill, delete every comment.
  Anatomy is fixed (STYLE.md); wording rules are in VOICE.md.
  Most task pages land between 200 and 500 words of prose plus steps.
  Past ~700 words, split the page.
*/}

# Verb-first task title

One-line value statement: why this task exists, stated as a fact about the
job, not praise for the product. (This is the one place the personality
budget may be spent.)

{/*
  WHO CAN DO THIS — always present, always before the steps.
  One line. Name the roles or the permission in plain language.
*/}

**Who can do this:** members with [e.g. project management] access.

{/*
  PREREQUISITES — only if genuinely required. Omit the block otherwise.
  Link, don't re-explain.
*/}

Before you start, [the thing that must exist first](/docs/section/page).

## Do the thing

{/*
  Numbered steps. Top-level ordered lists render as step badges.
  - Verb-first imperatives. Bold every UI label, exactly as on screen.
  - One action per step. A decision inside a step means it wants to be
    two steps or a table.
  - No promises of speed or ease inside steps (VOICE.md layer 1).
*/}

1. Go to **Somewhere** and click **The Button**.
2. Fill in the fields that matter. Name only the ones with consequences —
   the form explains itself for the rest.
3. Click **Save**.

_Screenshot: what the reader should be looking at right now._

:::tip
Optional. A shortcut or habit an experienced operator would mention.
:::

:::warning
Optional. The mistake people actually make, and its consequence. Only for
real, encountered footguns — not imaginable ones.
:::

## What happens next

Optional section: the downstream effects the reader can't see from the
screen they're on (what changed on the schedule, what the warehouse now
sees, what got locked). This is often the most valuable part of the page —
it's the part the UI can't tell them.

## Related

- [The adjacent task](/docs/section/page)
- [The owning concept](/docs/section/index-page)
