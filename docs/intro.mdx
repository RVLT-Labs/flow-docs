---
title: Theme preview
description: Internal page for checking the RVLT theme — not part of the guide.
unlisted: true
---

# Theme preview

Internal QA page — not part of the published guide (kept `unlisted`, so it's
off the sidebar and out of search, but still reachable by direct link for
checking the theme). The real topic pages linked from the homepage are the
actual guide; this one exists to sanity-check headings, body copy, links,
and the callout types below whenever the theme changes.

This is the **RVLT Flow** user guide — separate from the developer-facing
docs in the app repo. No real content has landed yet. The information
architecture (getting started, core concepts — projects, warehouse/
fulfillment, crew, assets/kits — and per-module guides) is tracked in
[gearflow#959](https://github.com/TwoToned/gearflow/issues/959).

:::note[What this page is for]
Use it to sanity-check the theme — headings, body copy, links, and the
callout types below — while real guide content is being written.
:::

## Callout types

:::tip
A tip callout — success green.
:::

:::info
An info callout — blue.
:::

:::warning
A warning callout — amber.
:::

:::danger
A danger callout — the one red accent, reserved for alerts.
:::

## Code

Inline `code` and fenced blocks use Geist Mono, the app's data typeface:

```ts
function loadOut(kit: string): boolean {
  return true;
}
```

## Numbered steps

A generic example (not real instructions) to check the stepper-styled
ordered list — top-level `<ol>` items get a numbered ring, matching a
walked-through guide instead of a plain numbered list:

1. Open the thing you're about to do this to.
2. Do the first part of the thing.
3. Confirm it looks right before moving on.
4. Do the last part, then check it off.
